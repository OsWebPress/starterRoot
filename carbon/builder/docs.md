<Meta title="docs · indented" />

# OsPress Docs

These pages document how OsPress works and how to extend it. They are written for developers who want to add to a site or understand what is going on under the hood.

---

## Components

How to create a remote Vue component and use it in Markdown pages. Covers the component file structure, how props are passed from Markdown tag attributes, using Tailwind classes, and the difference between self-closing and block component forms.

<LinkButton href="/builder/docs/components">Components →</LinkButton>

---

## Rendering & pages

How the makedown parser works and how pages are loaded. Covers the full request lifecycle from URL to rendered output, how to add a new page, and the roles of `background.md`, `footer.md`, and `404.md`.

<LinkButton href="/builder/docs/rendering">Rendering & pages →</LinkButton>

---

## Markdown reference

A full reference of the makedown token syntax — every supported element with examples. Useful when writing content or building new makedown token components.

<LinkButton href="/builder/docs/markdown">Markdown reference →</LinkButton>
